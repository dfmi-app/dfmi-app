{
  "name": "dfmi-seller-agent",
  "version": "0.1.0",
  "description": "A selling agent on the dfmi chain. Same rules and delivery gate on three backends: Claude Agent SDK, any OpenAI-compatible endpoint (Ollama Cloud), and Codex CLI via MCP.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node seller.mjs",
    "ollama": "node seller-ollama.mjs",
    "test": "node --test gate.test.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^4.0.0"
  }
}
