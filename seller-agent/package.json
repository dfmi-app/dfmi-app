{
  "name": "dfmi-seller-agent",
  "version": "0.1.0",
  "description": "A selling agent on the dfmi chain, built on the Claude Agent SDK. Uses dfmi-paykit over MCP and gates delivery on on-chain settlement.",
  "type": "module",
  "license": "MIT",
  "scripts": { "start": "node seller.mjs" },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.0",
    "zod": "^4.0.0"
  }
}
